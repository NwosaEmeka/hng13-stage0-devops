name: Deploy to EC2 via Nginx

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy HTML to EC2
        env:
          HOST: ${{ secrets.EC2_HOST }}
          USER: ${{ secrets.EC2_USER }}
          SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          echo "$SSH_KEY" > private_key.pem
          chmod 600 private_key.pem

          # Copy your HTML file to the Nginx web root directly
          scp -i private_key.pem -o StrictHostKeyChecking=no index.html $USER@$HOST:/usr/share/nginx/html/index.html

          # Adjust permissions and restart Nginx
          ssh -i private_key.pem -o StrictHostKeyChecking=no $USER@$HOST << 'EOF'
            sudo chown nginx:nginx /usr/share/nginx/html/index.html || sudo chown www-data:www-data /usr/share/nginx/html/index.html
            sudo systemctl restart nginx
          EOF

          rm -f private_key.pem
